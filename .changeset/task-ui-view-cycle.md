---
"@mblarsen/pi-task-ui": major
---

Change Alt+U and `/task-ui cycle` to cycle through the sidebar, browser, and hidden states. Add `/task-ui sidebar`, `/task-ui browse`, and `/task-ui hide` to select a state directly. In browse mode, q or Escape returns to the sidebar, while Alt+U hides both views.
